# 模板目录

本目录是题本模板源码的唯一来源，目录结构固定为：

```text
templates/<template_key>/<version>/
  manifest.toml
  main.tex.j2
```

版本号必须使用 `x.y.z` 格式。已发布版本不可原地修改，模板变更需要新增版本目录。后端会记录模板版本和目录摘要，渲染执行器在任务开始前校验二者，防止不同机器使用了内容不一致的模板。
